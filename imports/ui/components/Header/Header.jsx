import React from 'react';
import { ItemsCounter } from './ItemsCounter.jsx';
/*
 * It will only display the header above the Navigation Bar
 * It also contains the items counter component that display total number of items in the cart
 */
export const Header = () => (
    <div>

      
      <ItemsCounter />
    </div>
);
